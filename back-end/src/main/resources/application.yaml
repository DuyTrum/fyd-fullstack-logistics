server:
  port: 8080
  servlet:
    encoding:
      charset: UTF-8
      force: true
      enabled: true

spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/fyd_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Ho_Chi_Minh&characterEncoding=UTF-8&useUnicode=true}
    username: ${SPRING_DATASOURCE_USERNAME:fyd}
    password: ${SPRING_DATASOURCE_PASSWORD:fyd123}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      connection-init-sql: SET NAMES utf8mb4

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  jackson:
    serialization:
      write-dates-as-timestamps: false
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Ho_Chi_Minh

  # Email Configuration (Gmail)
  mail:
    host: smtp.gmail.com
    port: 587
    username: duytd24@gmail.com
    password: ymsznqyfwzabjdom
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000
        debug: false
    default-encoding: UTF-8

# App Configuration
app:
  name: FYD Store
  backend-url: ${APP_BACKEND_URL:http://localhost:8080}
  frontend-url: ${APP_FRONTEND_URL:http://localhost:5175}

# VNPay Configuration (Sandbox)
vnpay:
  tmn-code: 66AGBGDY
  hash-secret: DXFA3B3BZCJ2D015EH5HDB2OIEM4N3XC
  api-url: https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
  return-url: ${app.frontend-url}/shop/payment-callback
  ipn-url: ${app.backend-url}/api/payments/vnpay/ipn

# GHTK Configuration (Sandbox)
ghtk:
  api-token: ${GHTK_API_TOKEN:}
  api-url: https://services.ghtk.vn/services/shipment/order

# MoMo Configuration (Sandbox - Test credentials from MoMo docs)
momo:
  partner-code: MOMO
  access-key: ${MOMO_ACCESS_KEY:F8BBA842ECF85}
  secret-key: ${MOMO_SECRET_KEY:K951B6PE1waDMi640xX08PD3vg6EkVlz}
  api-url: https://test-payment.momo.vn/v2/gateway/api/create
  return-url: ${app.frontend-url}/shop/payment-callback
  ipn-url: ${app.backend-url}/api/payments/momo/ipn

# OAuth Configuration
# Replace with actual credentials from Google/Facebook Developer Console
oauth:
  google:
    client-id: 1088282737098-b064q3m4tq72oghh6pq9uf9jl9gaq45b.apps.googleusercontent.com
  facebook:
    app-id: 1092239965343475
    app-secret: 720e3524448897994d58851452380430

# AI Configuration (Groq)
groq:
  api:
    key: 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
    model: llama-3.3-70b-versatile
    url: https://api.groq.com/openai/v1/chat/completions

logging:
  level:
    com.fyd.backend: DEBUG
    org.hibernate.SQL: DEBUG
    org.springframework.web: DEBUG
